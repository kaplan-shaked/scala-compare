name: Continuos Integration
on:
  pull_request:
    branches: ['**']
  push:
    branches: ['**', '!update/**', '!pr/**']

jobs:

  test-action-itself:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        id: test-gh-action
        with:
          files: 'resources/v1.scala_test,ci_test_file2.scala_test'
      - name: Check on failures
        if: ${{ steps.test-gh-action.outputs.result }} == 'true'
        run: exit 0